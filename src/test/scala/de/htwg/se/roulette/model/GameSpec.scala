package de.htwg.se.roulette.model

import org.scalatest._

class GameSpec extends WordSpec with Matchers {
  "A number" when {
    "is even"in {
      Boolean b = new G
      x.isEven should be(true)
    }
  }

}
